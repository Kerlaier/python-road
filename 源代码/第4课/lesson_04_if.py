# -*- coding: utf-8 -*-
import sys
sys.stdout.reconfigure(encoding='utf-8')
"""
============================================================
  Python 从入门到精通 —— 第4课：条件判断 if/elif/else
  实战场景：根据分数评等级、根据消费给折扣
============================================================
"""

# ============================================================
# 一、if —— 满足条件才执行
# ============================================================
print("[场景1] 判断是否及格")
print("-" * 30)

# 语法公式：
#   if 条件:
#       缩进4格，条件为 True 时执行的代码

# 翻译成人话：如果分数 >= 60，就打印"及格"
score = 75

if score >= 60:
    print("恭喜，及格了！")    # score=75 满足条件，会执行这行

# 这里没有缩进，不属于 if 了，一定会执行
print("判断完毕")
print()

# score=40 时：
score = 40
if score >= 60:
    print("恭喜，及格了！")    # 40 < 60，不满足，跳过这行
print("判断完毕（分数40，没有打印及格）")
print()


# ============================================================
# 二、if/else —— 二选一
# ============================================================
print("[场景2] 是否成年")
print("-" * 30)

# 语法公式：
#   if 条件:
#       条件为 True 时执行
#   else:
#       条件为 False 时执行

age = 16

if age >= 18:
    print("已成年，可以进入")     # 16 < 18，不执行
else:
    print("未成年，禁止进入")     # 所以走这条

# 翻译成人话：
#   if    = 如果...就
#   else  = 否则...就
# 两条路必然走一条


# ============================================================
# 三、if/elif/else —— 多选一
# ============================================================
print("[场景3] 成绩等级评定")
print("-" * 30)

# 语法公式：
#   if 条件1:
#       条件1为 True 时执行
#   elif 条件2:
#       条件1为 False，条件2为 True 时执行
#   elif 条件3:
#       前面都为 False，条件3为 True 时执行
#   else:
#       前面全部为 False 时执行

score = 85

if score >= 90:
    grade = "A"
elif score >= 80:
    grade = "B"           # 85 >= 80，走这条！后面的都不看
elif score >= 70:
    grade = "C"
elif score >= 60:
    grade = "D"
else:
    grade = "F"

# 翻译成人话：
#   if    = 如果 >=90  → A
#   elif  = 否则如果 >=80 → B
#   elif  = 否则如果 >=70 → C
#   else  = 都不满足 → F

print(f"分数 {score} 分，等级：{grade}")

# *** 关键规则：从上到下依次判断，命中一条就跳出！***
# 即使 85 也满足 ">=70" 和 ">=60"，但只会执行第一个命中的 ">=80"


# ============================================================
# 四、实战：商场会员折扣系统
# ============================================================
print("\n[场景4] 商场会员折扣")
print("-" * 30)

# 规则：
#   钻石会员 → 打7折
#   金卡会员 → 打8折
#   银卡会员 → 打9折
#   普通用户 → 不打折
#   消费满500额外再减50

member_level = "金卡"    # 用户的会员等级
amount = 600             # 消费金额

# 第一步：根据等级给折扣
if member_level == "钻石":
    rate = 0.7
elif member_level == "金卡":
    rate = 0.8           # 金卡，打八折
elif member_level == "银卡":
    rate = 0.9
else:
    rate = 1.0           # 普通用户不打折

discounted = amount * rate
print(f"会员等级：{member_level}，原价{amount}元")
print(f"折后：{discounted}元（{rate*10}折）")

# 第二步：满500额外减50（嵌套 if）
if discounted >= 500:
    discounted = discounted - 50
    print(f"满500减50：{discounted}元")

print(f"最终付款：{discounted}元")


# ============================================================
# 五、条件组合 —— and / or / not
# ============================================================
print("\n[场景5] 登录验证")
print("-" * 30)

# and = 两个条件都满足
# or  = 两个条件满足一个
# not = 反过来

username = "admin"
password = "123456"

# 模拟用户输入
input_user = "admin"
input_pass = "wrong"

if input_user == username and input_pass == password:
    print("登录成功！")
else:
    print("用户名或密码错误")    # 密码错了，走这里

# 判断是不是 VIP 或 消费超过1000
is_vip = False
amount = 1200

if is_vip or amount >= 1000:
    print("享受优先配送！")     # 不是VIP但消费超了，也能享受


# ============================================================
# 本课小结
# ============================================================
print("\n" + "=" * 50)
print("[总结]")
print("=" * 50)
print("""
语法公式：
  if 条件1:
      代码A           ← 条件1为 True 才执行
  elif 条件2:
      代码B           ← 条件1为 False，条件2为 True 才执行
  elif 条件3:
      代码C           ← 前面全 False，条件3为 True 才执行
  else:
      代码D           ← 前面全 False 才执行（可省略）

关键规则：
  1. 从上到下依次判断，命中一条就跳出
  2. 缩进（Tab）决定哪些代码属于 if
  3. elif 和 else 可省略，if 必须保留
  4. 条件可以用 and/or/not 组合

下一课：循环 for / while —— 重复执行代码
""")
